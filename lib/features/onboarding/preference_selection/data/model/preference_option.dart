import 'dart:ui';

class PreferenceOption {
  final String label;
  final String asset;
  // final Locale locale;
  const PreferenceOption({
    required this.label,
    required this.asset,
    // required this.locale,
  });
}
